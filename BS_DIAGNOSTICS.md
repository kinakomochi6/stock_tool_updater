# B/S精度検証

貸借対照表の抽出精度は、特定銘柄だけに合わせず、固定した回帰セットと未検証の拡張セットで確認します。

## テストセット

- `regression-40`: これまで改善に使用した40銘柄。既存精度の回帰検出用です。
- `expansion-60`: 既存40銘柄と重複しない60銘柄。業種、金融機関、インフラ、会計基準の違いを広げます。
- `breadth-100`: 既存100銘柄と重複しない100銘柄。33業種、3市場区分、PRO Market、英字を含む新証券コードを含みます。
- `market-100`: 上記2セットを合わせた100銘柄です。
- `market-200`: `market-100` と `breadth-100` を合わせた200銘柄です。

GitHub Actionsの `Update Stock Data` を手動実行し、次の値を設定します。

```text
codes      空欄
test_set   expansion-60
days_back  365
debug_bs   true
dry_run    true
```

既存40銘柄も同時に確認する場合は `test_set` を `market-100` にします。市場横断の代表抽出は `breadth-100`、金融・不動産・運輸などを厚くした高難度検証は `stress-100`、全テスト群を通す場合は `market-300` を選びます。追加で特定銘柄を調べる場合は、`codes` にカンマ区切りで入力すると選択したテストセットへ追加されます。

## 判定指標

診断では「その他」項目について、計算差額とXBRLで報告された値との差を絶対値で評価します。プラスとマイナスを相殺しません。

実行後のActions Summaryと `bs-diagnostics` artifactに、次の集計が出力されます。

- 成功・失敗銘柄数
- J-GAAP、IFRS、US GAAPの件数
- 連結・単体contextの件数
- 絶対残差が1億、10億、100億、1,000億円を超えた銘柄数
- 残差上位銘柄と対象カテゴリ
- 警告件数

ローカルの診断JSONも同じ方法で集計できます。

```bash
python bs_diagnostics_report.py diagnostics
```

## 改善サイクル

1. `regression-40` で既存精度を維持できていることを確認します。
2. `expansion-60` の残差上位をXBRL構造別に分類します。
3. `breadth-100` で33業種と市場区分を横断し、新しい開示形式を検出します。
4. 複数企業へ適用できる分類・親子関係・重複排除だけを実装します。
5. `market-200` で回帰と一般化を再確認します。
6. 安定後に対象を段階的に増やし、最終的に全上場銘柄の分布を監視します。
